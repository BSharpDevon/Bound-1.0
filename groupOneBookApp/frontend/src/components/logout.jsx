

const Logout = ()=>{
    return (
        <div>
            <input
                name="logout"
                type="button"
                value="LOG OUT"
                onClick={handleLogout}
                id="logoutButton"
            />
        </div>
    )
}